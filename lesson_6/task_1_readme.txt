Для тестов взял задачу номер 5 из практического задания к уроку 3 (найти максимальное отрицательное число).
Я надеюсь, что все правильно понял, и не нужно было вычислять размер всех объектов в списке list.

Я добавил два варианта - с кортежем и с генератором. Определенно, вариант с генератором будет оптимальным
в плане использования оперативной памяти, даже при условии, что мне пришлось добавить лишнюю переменную, которая
заменила мне индекс числа, потому что в единицу времени он выдает только одно значение, а
кортеж и список хранят в себе сразу много объектов. Однако, при использовании кортежа, все равно будет небольшая
экономия в памяти, т.к. в "коробке" у списка есть дополнительно занимаемые ячейки памяти, которые используются
для его расширения, тогда как кортеж всегда имеет фиксированный размер при его создании.